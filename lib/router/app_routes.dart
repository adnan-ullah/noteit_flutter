abstract class Routes {
  Routes._();

  static const PRODUCTS = Paths.PRODUCTS;
  static const NOTEPAGE = Paths.NOTEPAGE;
}

abstract class Paths {
  static const PRODUCTS = '/product';
  static const NOTEPAGE = '/note';
}